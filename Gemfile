source "https://rubygems.org"

# Built by GitHub Actions (see .github/workflows/jekyll.yml), so we are not
# limited to the github-pages gem's plugin whitelist.
gem "jekyll", "~> 4.3.4"

# Required on Ruby 3.4+ where these left the standard library.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
gem "ostruct"
